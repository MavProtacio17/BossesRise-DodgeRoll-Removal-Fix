# Normalize line endings across platforms
* text=auto eol=lf

# Windows batch scripts must stay CRLF
*.bat text eol=crlf

# The Gradle wrapper script must stay LF (breaks on Linux/macOS otherwise)
gradlew text eol=lf

# Binary files — never touch line endings
*.jar binary
*.png binary
*.zip binary
